{
    "name": "bazel_playground",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "@bazel/bazelisk": "latest",
        "@bazel/ibazel": "latest",
        "@bazel/buildifier": "latest"
    },
    "scripts": {
        "clean": "bazel clean",
        "build": "bazel build //...",
        "test": "bazel test //...",
        "run:example": "bazel run //packages/example:myscript"
    }
}
